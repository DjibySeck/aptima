{% extends 'base.html.twig' %}

{% block title %}StatutAgent{% endblock %}

{% block body %}
    <h1>StatutAgent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_agent.id }}</td>
            </tr>
            <tr>
                <th>Agent</th>
                <td>{{ statut_agent.user }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ statut_agent.dateDebut ? statut_agent.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ statut_agent.dateFin ? statut_agent.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ statut_agent.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_agent_index') }}">back to list</a>

    <a href="{{ path('app_statut_agent_edit', {'id': statut_agent.id}) }}">edit</a>

    {{ include('statut_agent/_delete_form.html.twig') }}
{% endblock %}
