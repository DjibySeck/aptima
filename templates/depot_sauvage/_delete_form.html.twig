<form method="post" action="{{ path('app_depot_sauvage_delete', {'id': depot_sauvage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depot_sauvage.id) }}">
    <button class="btn">Delete</button>
</form>
