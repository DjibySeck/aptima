<form method="post" action="{{ path('app_reseau_incendie_delete', {'id': reseau_incendie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reseau_incendie.id) }}">
    <button class="btn">Delete</button>
</form>
